[project]
name = "aquant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.41.2",
    "pydantic>=2.0",
    "structlog>=26.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.16",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
